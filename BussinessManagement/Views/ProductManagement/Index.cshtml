@using BussinessManagement.Models;
@model IEnumerable<Product>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    WebGrid grid = new WebGrid(source: Model, rowsPerPage: 10);
}
@Html.ActionLink("Add new product", "AddProduct", null, new { @class = "btn btn-success" })
<br />
<br/>
@grid.GetHtml(tableStyle: "gridTable",
    headerStyle: "gridHeader",
    footerStyle: "gridFooter",
    rowStyle: "gridRows",
    alternatingRowStyle: "gridAltRows",
    columns: grid.Columns(
    grid.Column("ID", "ID"),
    grid.Column("Name", "Name"),
    grid.Column("Description", "Description", format: n => Html.Raw(n.Description)),
    grid.Column("Image", "Image", format: n => Html.Raw("<img src=\"../Content/ProductImage/" + n.Image + "\">")),
    grid.Column(format: p=> @Html.ActionLink("Edit","Edit",new {id=p.ID})),
    grid.Column(format: p => @Html.ActionLink("Delete", "Delete", new { id = p.ID }))
))
<link href="~/Content/admin/css/style-grid.css" rel="stylesheet" />


